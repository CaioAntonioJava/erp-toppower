# Artefatos temporários de execução local (não versionar)
/PID
# ZCode - arquivos de configuração e planos
.zcode/
.zcode*
/AGENTS.md
# Uploads locais (logos das Organizations)
/uploads/
/backend/uploads/
/backend/target/

# Secrets / variáveis de ambiente
.env
.env.*
!.env.example